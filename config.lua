local addon, ns = ...

local config = {
	buttonSize = 24,
	spacing = 4,
	screenPadding = 10,
	currencies = {
		823, -- apexis
		824,  -- garrison resources
		994, -- seal of tempered fate
	}
}

ns.config = config
